# ADR

- `0001-workspace-architecture.md`: documents the monorepo and workspace split used by the current implementation.
- `0002-desktop-backend-module-boundaries.md`: documents the Tauri backend runtime boundaries now implemented in the desktop crate.
- `0003-desktop-ui-stack-exception.md`: documents the accepted exception that keeps the current Tailwind-based custom `ui-kit` instead of forcing an immediate shadcn/Radix rewrite.
