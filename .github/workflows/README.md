# GitHub Workflows

- `frontend-build.yml`: validates the desktop frontend typecheck and production build.
- `rust-validation.yml`: validates the Rust workspace crates and desktop crate command/runtime surface.
- `frontend-e2e.yml`: runs the browser-mode Playwright suite for the desktop frontend.
