# Field Pane

Active feature module for:

- inferred column metadata display
- semantic-query dimension and measure selection
- field alias and row-limit editing

`ui/FieldPane.tsx` is the live workbench component consumed by `chart-builder/ui/QueryWorkspace.tsx`.
