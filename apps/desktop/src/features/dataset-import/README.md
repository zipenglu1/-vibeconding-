# Dataset Import

This feature owns the live dataset import flow for the desktop workbench.

It now contains:
- CSV path input and load trigger UI
- file-picker entrypoint wiring
- loaded data-source list and active-source summary
- preview rendering through `DataPreviewTable`
