# Filter Bar

Active feature module for semantic-query filter editing.

`ui/FilterBar.tsx` owns the live workbench controls for enabling filters and editing the filter field, operator, and value for the active data source.
