# Shared Lib

Shared frontend runtime helpers and utility functions belong here.

This layer now includes the workbench runtime hooks and helpers that feed the page/widget
assemblies, such as state wiring, derived-model hooks, effects, interaction helpers, and
the final `useWorkbenchRuntime.ts` entrypoint.
